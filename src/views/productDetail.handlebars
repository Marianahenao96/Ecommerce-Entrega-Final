<div class="container mt-5">
  <div class="row">
    <!-- Imagen del producto -->
    <div class="col-md-6">
      <img 
        src="{{product.thumbnails.[0]}}" 
        alt="{{product.title}}" 
        class="img-fluid rounded shadow-sm"
        onerror="this.src='/img/no-image.png'"
      >
    </div>

    <!-- Información del producto -->
    <div class="col-md-6">
      <h2 class="mb-3">{{product.title}}</h2>
      <p class="text-muted">{{product.description}}</p>

      <h4 class="text-primary fw-bold mb-3">${{product.price}}</h4>
      <p><strong>Stock:</strong> {{product.stock}}</p>
      <p><strong>Categoría:</strong> <span class="badge bg-info">{{product.category}}</span></p>
      <p><strong>Código:</strong> {{product.code}}</p>
      <p><strong>Estado:</strong> {{#if product.status}}Disponible ✅{{else}}No disponible ❌{{/if}}</p>

      <!-- Botones -->
      <div class="mt-4 d-flex gap-3">
        <form action="/api/carts/ID_DEL_CARRITO/products/{{product._id}}" method="post">
          <button type="submit" class="btn btn-success btn-lg">
            🛒 Agregar al carrito
          </button>
        </form>
        <a href="/api/products?page=1" class="btn btn-outline-secondary btn-lg">⬅️ Volver a productos</a>
      </div>
    </div>
  </div>
</div>
